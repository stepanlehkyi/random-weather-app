apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.apiConfig.name }}
  labels:
    app: {{ .Values.api.nameOverride }} # Reusing the label from earlier for consistency
data:
  {{- range $key, $value := .Values.apiConfig.data }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: weather-ui
  namespace: weather-system
spec:
  interval: 1m
  targetNamespace: weather-system
  chart:
    spec:
      chart: .helm/client
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
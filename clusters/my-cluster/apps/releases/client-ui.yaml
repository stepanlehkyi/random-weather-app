apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: weather-ui
  namespace: default
spec:
  interval: 1m
  targetNamespace: default
  chart:
    spec:
      chart: .helm/client
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system